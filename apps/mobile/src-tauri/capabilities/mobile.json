{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "mobile",
  "description": "Camera scanning — Android/iOS only, since tauri-plugin-barcode-scanner doesn't compile (let alone link) on desktop.",
  "platforms": ["android", "iOS"],
  "windows": ["main"],
  "permissions": ["barcode-scanner:default"]
}
