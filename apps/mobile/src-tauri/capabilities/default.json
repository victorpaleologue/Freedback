{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window: core events + the deep-link read API.",
  "windows": ["main"],
  "permissions": ["core:default", "deep-link:default"]
}
